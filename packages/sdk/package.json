{
  "name": "@actbound/sdk",
  "private": true,
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "openapi:generate": "tsx src/openapi/generate.ts"
  },
  "dependencies": {
    "@actbound/authorization": "workspace:*",
    "@asteasolutions/zod-to-openapi": "^7.3.2",
    "zod": "^3.24.2"
  }
}

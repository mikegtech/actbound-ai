# Infra Notes

- `docker-compose.yml` provisions Redis locally for future orchestration state, queues, or ephemeral caching.
- Infrastructure-specific deployment assets can be added here without coupling them to the application packages.
